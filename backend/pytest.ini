[pytest]
testpaths = tests
pythonpath = .
addopts = -q --strict-markers
markers =
    ml: requires PyTorch and the inference stack (skipped when unavailable)
filterwarnings =
    ignore::DeprecationWarning
